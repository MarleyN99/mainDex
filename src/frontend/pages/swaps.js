import React from 'react';

const Swaps = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Swaps</h1>
    </div>
  );
};

export default Swaps;
import React from 'react';

const Transactions = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Transactions</h1>
    </div>
  );
};

export default Transactions;